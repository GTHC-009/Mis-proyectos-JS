import {
    número,
    btn1,
    avisos,
    revisar,
    copiar,
    borrar,
    sugerir,
  } from "script.js";

if (avisos !== "") {
    console.log("XD");
}

aviso.innerHTML = avisos.inicial;

btn1.addEventListener("click", borrar);

número.addEventListener("input", revisar);